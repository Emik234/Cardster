<% @projects.each do |project| %>
	<div class='project'>
		<h3 style= 'display: inline;'><%= link_to project.title, project %></h3>
			<span class='delete-item'>
				<%= link_to 'X', project_path(project), method: :delete, remote: true, data: {deletable: :project}%>
			</span>
		</div>
<% end %>
